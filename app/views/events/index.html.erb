<% provide(:title, "投稿一覧") %>

<div class="row">
  <div class="col-md-12">
    <div class="my-3 p-3 bg-white rounded shadow-sm">
      <h3 class="border-bottom border-gray pb-2 mb-0">イベント一覧</h3>
      <div class="row mb-2 mt-2">
        <%= @events.each do |event| %>
          <%= render "events/event_card", event: event %>
        <% end %>
      </div>
    </div>
  </div>
</div>

