<% provide(:title, "トップ") %>
<div class="jumbotron p-4 p-md-5 text-white rounded bg-dark">
    <div class="col-md-6 px-0">
      <h1 class="display-4 font-italic">SHARE SNOW</h1>
      <p class="lead my-3">ここではスキー、スノーボードを一緒にする仲間を募集したりあなたの雪情報をシェアできます！会員登録はこちらから！</p>
      <p class="lead mb-0">
        <%= link_to new_user_registration_path, class: "btn btn-success btn-sm" do %>
          <i class="fas fa-user"></i>会員登録
        <% end %>
        <%= link_to new_user_session_path, class: "btn btn-primary btn-sm" do %>
          <i class="fas fa-user"></i>ログイン
        <% end %>
      </p>

    </div>
  </div>
<div class="row">
  <div class="col-md-12">
      <h3 class="border-bottom border-gray pb-2 mb-0">新着イベント</h3>
      <div class="row mb-2 mt-2">
        <% @events.each do |event| %>
          <%= render "events/event_card", event: event %>
        <% end %>
      </div>
    </div>
</div>
<div class="row">
    <div class="col-md-8">
        <div class="my-3 p-3 bg-white rounded shadow-sm">
          <h3 class="border-bottom border-gray pb-2 mb-0">新着投稿</h3>
          <%= render "post_index", user_posts: @user_posts %>
          <small class="d-block text-right mt-3">
            <%= link_to "All suggestions", user_posts_path %>
          </small>
        </div>
    </div>
    <div class="col-md-4">
      <div class="my-3 p-3 bg-white rounded shadow-sm">
        <h3 class="border-bottom border-gray pb-2 mb-0">新着ユーザー</h3>
        <% @users.each do |user| %>
          <%= render "users/profile", user: user%>
        <% end %>
        <small class="d-block text-right mt-3">
          <%= link_to "All suggestions", user_posts_path %>
        </small>
      </div>
    </div>
</div>